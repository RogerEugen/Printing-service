<x-app-layout><x-slot name="header"><h1 class="text-2xl font-bold">Edit User</h1><p class="mt-1 text-sm text-slate-500">{{ $user->username }}</p></x-slot><div class="mx-auto max-w-2xl"><form method="POST" action="{{ route('admin.users.update', $user) }}" class="space-y-6 rounded-2xl border border-slate-200 bg-white p-8 shadow-sm">@include('admin.users._form', ['user' => $user])</form><form method="POST" action="{{ route('admin.users.destroy', $user) }}" class="mt-5 rounded-2xl border border-red-200 bg-red-50 p-5" onsubmit="return confirm('Delete or disable this user?')">@csrf @method('DELETE')<p class="text-sm text-red-800">Users with print history are disabled to preserve audit records.</p><button class="mt-3 rounded-xl border border-red-300 px-4 py-2 text-sm font-semibold text-red-700">Delete user safely</button></form></div></x-app-layout>
