<x-app-layout><x-slot name="header"><div class="flex items-center justify-between"><div><h1 class="text-2xl font-bold">Printers</h1><p class="mt-1 text-sm text-slate-500">Manage authenticated printer agents.</p></div><a href="{{ route('admin.printers.create') }}" class="rounded-xl bg-cyan-600 px-4 py-2.5 text-sm font-semibold text-white">Add Printer</a></div></x-slot><div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">@foreach ($printers as $printer)<a href="{{ route('admin.printers.show', $printer) }}" class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm hover:border-cyan-300"><div class="flex items-start justify-between"><h2 class="font-bold">{{ $printer->name }}</h2><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $printer->displayedStatus() === \App\Enums\PrinterStatus::Online ? 'bg-emerald-100 text-emerald-800' : ($printer->displayedStatus() === \App\Enums\PrinterStatus::Disabled ? 'bg-red-100 text-red-800' : 'bg-slate-200 text-slate-700') }}">{{ ucfirst($printer->displayedStatus()->value) }}</span></div><p class="mt-2 text-sm text-slate-500">{{ $printer->location ?: 'No location' }}</p><p class="mt-4 text-sm">{{ $printer->print_jobs_count }} jobs</p><p class="mt-1 text-xs text-slate-400">Last seen: {{ $printer->last_seen_at?->diffForHumans() ?? 'Never' }}</p></a>@endforeach</div><div class="mt-6">{{ $printers->links() }}</div></x-app-layout>
