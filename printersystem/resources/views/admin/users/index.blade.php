<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><div><h1 class="text-2xl font-bold">Users</h1><p class="mt-1 text-sm text-slate-500">Create and manage company accounts.</p></div><a href="{{ route('admin.users.create') }}" class="rounded-xl bg-cyan-600 px-4 py-2.5 text-sm font-semibold text-white">Create User</a></div></x-slot>
    <div class="overflow-x-auto rounded-2xl border border-slate-200 bg-white shadow-sm"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase text-slate-500"><tr><th class="px-6 py-3">Username</th><th class="px-6 py-3">Role</th><th class="px-6 py-3">Status</th><th class="px-6 py-3">Jobs</th><th class="px-6 py-3"></th></tr></thead><tbody class="divide-y divide-slate-100">@foreach ($users as $user)<tr><td class="px-6 py-4 font-semibold">{{ $user->username }}</td><td class="px-6 py-4 capitalize">{{ $user->role->value }}</td><td class="px-6 py-4"><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $user->is_active ? 'bg-emerald-100 text-emerald-800' : 'bg-slate-200 text-slate-700' }}">{{ $user->is_active ? 'Active' : 'Disabled' }}</span></td><td class="px-6 py-4">{{ $user->print_jobs_count }}</td><td class="px-6 py-4 text-right"><a class="font-semibold text-cyan-700" href="{{ route('admin.users.edit', $user) }}">Edit</a></td></tr>@endforeach</tbody></table></div><div class="mt-6">{{ $users->links() }}</div>
</x-app-layout>
