<x-app-layout>
    <x-slot name="header"><h1 class="text-2xl font-bold">All Print Jobs</h1><p class="mt-1 text-sm text-slate-500">Filter and audit company print activity.</p></x-slot>
    <form method="GET" class="mb-6 grid gap-4 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:grid-cols-4"><select name="status" class="rounded-xl border-slate-300"><option value="">All statuses</option>@foreach (\App\Enums\PrintJobStatus::cases() as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ $status->label() }}</option>@endforeach</select><select name="user_id" class="rounded-xl border-slate-300"><option value="">All users</option>@foreach ($users as $user)<option value="{{ $user->id }}" @selected((string) request('user_id') === (string) $user->id)>{{ $user->username }}</option>@endforeach</select><select name="printer_id" class="rounded-xl border-slate-300"><option value="">All printers</option>@foreach ($printers as $printer)<option value="{{ $printer->id }}" @selected((string) request('printer_id') === (string) $printer->id)>{{ $printer->name }}</option>@endforeach</select><button class="rounded-xl bg-slate-900 px-4 py-2 font-semibold text-white">Apply filters</button></form>
    <div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">@include('admin.print-jobs._table', ['printJobs' => $printJobs])</div><div class="mt-6">{{ $printJobs->links() }}</div>
</x-app-layout>
