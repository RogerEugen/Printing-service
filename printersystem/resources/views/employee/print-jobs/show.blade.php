<x-app-layout>
    <x-slot name="header"><div class="flex flex-wrap items-center justify-between gap-3"><div><h1 class="text-2xl font-bold">Print Job #{{ $printJob->id }}</h1><p class="mt-1 text-sm text-slate-500">{{ $printJob->original_name }}</p></div><x-status-badge :status="$printJob->status" class="px-3 py-1.5" /></div></x-slot>
    <div class="grid gap-6 lg:grid-cols-3">
        <section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm lg:col-span-2"><h2 class="font-semibold">Job details</h2><dl class="mt-5 grid gap-5 sm:grid-cols-2"><div><dt class="text-xs uppercase text-slate-500">Printer</dt><dd class="mt-1 font-medium">{{ $printJob->printer->name }}</dd></div><div><dt class="text-xs uppercase text-slate-500">Copies</dt><dd class="mt-1 font-medium">{{ $printJob->copies }}</dd></div><div><dt class="text-xs uppercase text-slate-500">File type</dt><dd class="mt-1 font-medium uppercase">{{ $printJob->file_extension }}</dd></div><div><dt class="text-xs uppercase text-slate-500">File size</dt><dd class="mt-1 font-medium"><x-file-size :bytes="$printJob->file_size" /></dd></div><div><dt class="text-xs uppercase text-slate-500">Created</dt><dd class="mt-1 font-medium">{{ $printJob->created_at->format('d M Y, H:i') }}</dd></div></dl>@if ($printJob->error_message)<div class="mt-6 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-800"><strong>Failure reason:</strong> {{ $printJob->error_message }}</div>@endif @if ($printJob->status === \App\Enums\PrintJobStatus::Pending)<form method="POST" action="{{ route('print-jobs.cancel', $printJob) }}" class="mt-6">@csrf<button class="rounded-xl border border-red-300 px-4 py-2 text-sm font-semibold text-red-700 hover:bg-red-50">Cancel pending job</button></form>@endif</section>
        <section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-semibold">Activity</h2><div class="mt-5 space-y-4">@forelse ($printJob->logs as $log)<div class="border-l-2 border-cyan-500 pl-3"><p class="text-sm font-medium">{{ str($log->event)->replace('_', ' ')->title() }}</p><p class="text-xs text-slate-500">{{ $log->message }}</p><time class="text-xs text-slate-400">{{ $log->created_at->format('d M H:i') }}</time></div>@empty<p class="text-sm text-slate-500">No activity logged.</p>@endforelse</div></section>
    </div>
</x-app-layout>
